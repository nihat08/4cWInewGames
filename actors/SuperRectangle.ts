import { Rectangle } from "./Rectangle.js";

export class SuperRectangle extends Rectangle {
    constructor(x: number, y: number, width: number, height: number) {
        super(x, y, width, height);
    }}